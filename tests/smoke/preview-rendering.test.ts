/**
 * Preview Rendering Smoke Test
 */

import { describe, it, expect } from 'vitest';

describe('Preview Rendering Smoke', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
