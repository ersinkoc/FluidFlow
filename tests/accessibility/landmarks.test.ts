/**
 * Landmarks Accessibility Test - Smoke Test
 */

import { describe, it, expect } from 'vitest';

describe('Landmarks Accessibility', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
