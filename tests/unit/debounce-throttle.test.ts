/**
 * Debounce & Throttle Unit Test - Smoke Test
 */

import { describe, it, expect } from 'vitest';

describe('Debounce & Throttle Unit', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
