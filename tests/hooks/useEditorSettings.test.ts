/**
 * useEditorSettings.test - Smoke Test
 */

import { describe, it, expect } from 'vitest';

describe('useEditorSettings.test', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
