/**
 * useVersionHistory.test - Smoke Test
 */

import { describe, it, expect } from 'vitest';

describe('useVersionHistory.test', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
