/**
 * projectScanner.test - Smoke Test
 */

import { describe, it, expect } from 'vitest';

describe('projectScanner.test', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
