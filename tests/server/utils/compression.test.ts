/**
 * compression.test - Smoke Test
 */

import { describe, it, expect } from 'vitest';

describe('compression.test', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
