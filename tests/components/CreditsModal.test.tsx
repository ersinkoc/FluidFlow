/**
 * CreditsModal Component - Smoke Test
 */

import { describe, it, expect } from 'vitest';
import { CreditsModal } from '../../components/CreditsModal';

describe('CreditsModal', () => {
  it('should be defined', () => {
    expect(CreditsModal).toBeDefined();
  });
});
