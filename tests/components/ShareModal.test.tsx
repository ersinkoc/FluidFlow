/**
 * ShareModal Component - Smoke Test
 */

import { describe, it, expect } from 'vitest';
import { ShareModal } from '../../components/ShareModal';

describe('ShareModal', () => {
  it('should be defined', () => {
    expect(ShareModal).toBeDefined();
  });
});
