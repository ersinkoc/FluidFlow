/**
 * InputDialog Component - Smoke Test
 */

import { describe, it, expect } from 'vitest';
import * as InputDialog from '../../components/InputDialog';

describe('InputDialog', () => {
  it('should export InputDialog component', () => {
    expect(InputDialog).toBeDefined();
  });
});
