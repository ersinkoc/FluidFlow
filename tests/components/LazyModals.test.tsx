/**
 * LazyModals Component - Smoke Test
 */

import { describe, it, expect } from 'vitest';
import * as LazyModals from '../../components/LazyModals';

describe('LazyModals', () => {
  it('should export LazyModals component', () => {
    expect(LazyModals).toBeDefined();
  });
});
