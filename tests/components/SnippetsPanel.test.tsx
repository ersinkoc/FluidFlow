/**
 * SnippetsPanel Component - Smoke Test
 */

import { describe, it, expect } from 'vitest';
import { SnippetsPanel } from '../../components/SnippetsPanel';

describe('SnippetsPanel', () => {
  it('should be defined', () => {
    expect(SnippetsPanel).toBeDefined();
  });
});
