/**
 * ResetConfirmModal Component - Smoke Test
 */

import { describe, it, expect } from 'vitest';
import * as ResetConfirmModal from '../../../components/ControlPanel/ResetConfirmModal';

describe('ResetConfirmModal', () => {
  it('should export ResetConfirmModal component', () => {
    expect(ResetConfirmModal).toBeDefined();
  });
});
