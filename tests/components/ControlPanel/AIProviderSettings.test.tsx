/**
 * AIProviderSettings Component - Smoke Test
 */

import { describe, it, expect } from 'vitest';
import { AIProviderSettings } from '../../../components/ControlPanel/AIProviderSettings';

describe('AIProviderSettings', () => {
  it('should be defined', () => {
    expect(AIProviderSettings).toBeDefined();
  });
});
