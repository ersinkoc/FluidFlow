/**
 * InputDialog - Smoke Test
 */

import { describe, it, expect } from 'vitest';
import * as InputDialog from '../../../components/InputDialog/index';

describe('InputDialog', () => {
  it('should export InputDialog component', () => {
    expect(InputDialog).toBeDefined();
  });
});
