/**
 * ContextMenu Component - Smoke Test
 */

import { describe, it, expect } from 'vitest';
import ContextMenu from '../../components/ContextMenu';

describe('ContextMenu', () => {
  it('should be defined', () => {
    expect(ContextMenu).toBeDefined();
  });
});
