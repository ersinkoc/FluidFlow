/**
 * CompactionPreviewModal Component - Smoke Test
 */

import { describe, it, expect } from 'vitest';
import CompactionPreviewModal from '../../components/CompactionPreviewModal';

describe('CompactionPreviewModal', () => {
  it('should be defined', () => {
    expect(CompactionPreviewModal).toBeDefined();
  });
});
