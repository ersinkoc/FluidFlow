/**
 * GeneratingOverlay Component - Smoke Test
 */

import { describe, it, expect } from 'vitest';
import * as GeneratingOverlay from '../../../components/PreviewPanel/GeneratingOverlay';

describe('GeneratingOverlay', () => {
  it('should export GeneratingOverlay component', () => {
    expect(GeneratingOverlay).toBeDefined();
  });
});
