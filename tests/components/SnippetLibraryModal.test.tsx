/**
 * SnippetLibraryModal Component - Smoke Test
 */

import { describe, it, expect } from 'vitest';
import * as SnippetLibraryModal from '../../components/SnippetLibraryModal';

describe('SnippetLibraryModal', () => {
  it('should export SnippetLibraryModal component', () => {
    expect(SnippetLibraryModal).toBeDefined();
  });
});
