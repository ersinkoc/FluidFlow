/**
 * providerFactory.test - Smoke Test
 */

import { describe, it, expect } from 'vitest';

describe('providerFactory.test', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
