import type { PromptCategory } from '../types';

export const dashboardCategory: PromptCategory = {
    id: 'dashboard',
    name: 'Dashboard',
    icon: 'LayoutDashboard',
    description: 'Dashboard and analytics components',
    prompts: [
      {
        id: 'da1',
        label: 'Dashboard Layout',
        simple: 'Create dashboard layout with sidebar navigation, header, and main content area.',
        detailed: 'Create complete dashboard layout structure. Sidebar: fixed left (w-64), collapsible to icons-only (w-16), contains logo, navigation with icons, user profile at bottom. Header: fixed top with h-16, search bar, notifications bell with badge, user avatar dropdown. Main content: flex-1 with overflow-auto, padding (p-6). Grid layout for widgets: grid-cols-1 md:grid-cols-2 xl:grid-cols-4 gap-6. Responsive: sidebar becomes overlay drawer on mobile. Dark mode support.',
        advanced: 'Create complete dashboard layout structure with full responsiveness. Sidebar: fixed left (w-64), collapsible to icons-only mode (w-16) with toggle button (ChevronLeft/ChevronRight icon), smooth transition-all duration-300. Sidebar content: logo/brand at top, navigation links with lucide icons and labels (Dashboard, Analytics, Users, Settings), grouped sections with collapsible headers, user profile at bottom (avatar, name, role, logout). Header: fixed top (top-0 z-40), h-16 bg-white border-b, flex items-center justify-between px-6. Header content: hamburger menu (mobile only), search bar (w-72 hidden md:block), quick action buttons, notifications bell (Bell icon) with unread count badge (bg-red-500 text-white text-xs rounded-full), user avatar dropdown with profile link, settings, logout. Main content: ml-64 (or ml-16 when collapsed) pt-16, flex-1 overflow-auto, padding (p-6), bg-gray-50 or bg-gray-100. Grid layout for widgets: grid-cols-1 md:grid-cols-2 xl:grid-cols-4 gap-6. Responsive: on mobile (md:hidden) sidebar transforms to overlay drawer (fixed inset-0 z-50) with dark backdrop, hamburger menu in header toggles drawer. Breadcrumbs below header for navigation context (Home > Dashboard > Analytics). Content sections with consistent card styling (bg-white rounded-lg shadow-sm p-6). Dark mode support: dark:bg-gray-900 dark:text-white throughout. Loading states for all sections. Error boundaries for widget failures. Focus trap when sidebar drawer is open on mobile.'
      },
      {
        id: 'da2',
        label: 'Metrics Cards',
        simple: 'Create KPI cards showing metric value with trend indicator (up/down arrow).',
        detailed: 'Build KPI/metrics card components for dashboard overview. Card structure: bg-white rounded-lg shadow-sm p-6. Content: metric label (text-sm text-gray-500), large value (text-3xl font-bold), trend indicator. Trend display: arrow icon (TrendingUp/TrendingDown), percentage change, colored text (green for positive, red for negative). Optional: comparison text ("vs last month"), icon representing the metric. Grid layout: 4 cards in row on desktop, 2 on tablet, 1 on mobile. Loading state: skeleton with pulsing animation.',
        advanced: 'Build KPI/metrics card components for dashboard overview with comprehensive features. Card structure: bg-white rounded-lg shadow-sm hover:shadow-md transition-shadow p-6, optional colored left border (border-l-4 border-blue-500) for accent. Content: metric label (text-sm text-gray-500 uppercase tracking-wide), large value (text-3xl font-bold text-gray-900, format numbers with toLocaleString), trend indicator below value. Trend display: flex items-center gap-1, TrendingUp (text-green-600) or TrendingDown (text-red-600) icon from lucide, percentage change (+12.5% or -3.2%), colored text matching trend. Comparison text: "vs last month" or "vs last week" in text-gray-400 text-xs. Optional elements: icon representing metric (Users, DollarSign, ShoppingCart, Activity) in colored circle (w-12 h-12 rounded-full bg-blue-100) positioned top-right or left. Mini sparkline chart: small 50x20px line chart showing 7-day trend using SVG path or chart library. Variants: primary (larger, full-width), compact (smaller padding, inline layout), with action button ("View details" link). Click behavior: entire card clickable, navigates to detailed view/report. Grid layout: grid-cols-1 sm:grid-cols-2 xl:grid-cols-4 gap-4 md:gap-6. Loading state: skeleton with animate-pulse, show gray rectangles matching content layout. Error state: show error icon with retry button. Real-time updates: optionally poll for updates or use WebSocket. Dark mode: dark:bg-gray-800 dark:text-white. Accessibility: meaningful card labels, announce value changes.'
      },
      {
        id: 'da3',
        label: 'Line Chart',
        simple: 'Create line chart showing data trends over time with hover tooltips.',
        detailed: 'Create line chart component for time-series data visualization. Container: responsive width, fixed or aspect-ratio height. Chart elements: SVG-based or canvas with chart library. Lines: smooth curves, multiple series with different colors. Axes: X-axis with time labels, Y-axis with value scale and grid lines. Tooltip: show values on hover with date. Legend: series names with color indicators. Time range selector: buttons for 7D, 30D, 90D, 1Y. Loading state: skeleton placeholder.',
        advanced: 'Create line chart component for time-series data visualization using Recharts, Chart.js, or custom SVG. Container: responsive width (w-full), aspect-ratio height (aspect-[16/9] or fixed h-80), bg-white rounded-lg p-4. Chart elements: SVG-based with ResponsiveContainer wrapper. Lines: smooth curves (type="monotone") or straight segments, multiple series support (up to 5) with distinct colors from accessible palette. Stroke width 2-3px, optional area fill with gradient (linearGradient from color to transparent). Axes: X-axis with time labels (formatted dates: "Jan 1", "Mon", etc.), Y-axis with value scale, grid lines (stroke-dasharray="3 3" in light gray), axis labels and tick marks. Data points: optional dots on line (r={4}), larger dots highlighted on hover (r={6}). Tooltip: custom tooltip component appearing on hover, shows date and all series values with color indicators, semi-transparent background with shadow. Legend: below chart, horizontal layout, series names with color circles, click to toggle series visibility (strike-through when hidden). Interactions: hover for tooltip, optional zoom/pan for large datasets using brush component. Responsive: reduce point density (show every Nth point) on smaller screens, simplify axis labels. Time range selector: button group (7D, 30D, 90D, 1Y, Custom), active state highlighted, triggers data refetch. Annotations: optional reference lines for targets/thresholds. Loading state: skeleton chart placeholder matching dimensions. Empty state: "No data available" message with illustration. Animation: lines draw in on initial load. Accessibility: provide data table alternative, proper ARIA labels. Dark mode: adjust colors for dark background.'
      },
      {
        id: 'da4',
        label: 'Bar Chart',
        simple: 'Create bar chart comparing values across categories.',
        detailed: 'Build bar chart component for categorical comparisons. Layout: vertical bars (default) or horizontal bars option. Bars: consistent width with gap between, colored fills, hover highlight. Labels: category names on X-axis, values on Y-axis with grid lines. Tooltip: show category and value on hover. Legend: for multi-series charts. Animation: bars grow from zero on initial load. Sorting: option to sort by value. Responsive: horizontal bars on mobile.',
        advanced: 'Build bar chart component for categorical comparisons with full interactivity. Layout: vertical bars (default) or horizontal bars option (better for long category names or mobile). Using Recharts BarChart or custom SVG. Bars: consistent width calculated based on data length with gap between (barGap={4}), colored fills (fill prop from palette), hover highlight (opacity increase or brightness), border-radius on tops (radius={[4, 4, 0, 0]}). Labels: category names on X-axis (angle={-45} if many categories, textAnchor="end"), values on Y-axis with grid lines (cartesianGrid strokeDasharray="3 3"), proper axis formatting (currency, percentage, abbreviated numbers: 1K, 1M). Value labels: optional labels on top of bars showing exact values, show on hover or always visible. Grouped bars: multiple series side by side with different colors, grouped by category. Stacked bars: series stacked on top of each other (stackId="a"), showing composition and total. Tooltip: custom tooltip showing category name and all series values, color-coded. Legend: positioned below or right, color swatches + labels, click to toggle series. Animation: bars grow from zero with duration 500ms on initial load, update animations on data change. Sorting: dropdown to sort by value ascending/descending or alphabetically, animate bar position changes. Responsive: horizontal bars work better on mobile (swap axes), reduce bar count or enable horizontal scroll. Negative values: bars extending below zero line, different color for negative. Click handler: onClick per bar triggers drill-down or filter. Reference lines: show target or average line. Loading and empty states. Accessibility: data table fallback. Dark mode support.'
      },
      {
        id: 'da5',
        label: 'Pie/Donut Chart',
        simple: 'Create pie or donut chart showing proportions of different categories.',
        detailed: 'Create pie or donut chart for proportion visualization. SVG-based chart with arc segments. Donut variant: inner radius creates hole for center content. Segments: colored arcs representing percentages. Center content (donut): total value. Legend: list of categories with color swatches. Hover effects: segment highlight with tooltip. Animation: segments animate in clockwise on load. Click handler: select segment for filtering.',
        advanced: 'Create pie or donut chart for proportion visualization using Recharts PieChart or custom SVG. SVG-based chart with arc segments calculated using d3-shape or built-in. Donut variant: inner radius (innerRadius={60}) creates hole for center content. Size: responsive, typically 200-300px diameter. Segments: colored arcs representing percentages using accessible color palette, separated by small gaps (paddingAngle={2}), stroke for definition. Center content (donut only): total value (text-2xl font-bold), percentage of selected, or meaningful icon/label, update on hover to show hovered segment. Labels: segment labels with percentages, positioned outside (label line) or as separate legend. Use label component for custom positioning. Legend: below or right side, list of categories with color swatches (w-3 h-3 rounded-full inline-block) and values/percentages, click legend item to toggle segment visibility. Hover effects: segment scales slightly (scale: 1.05) or increases brightness, tooltip appears showing category, value, percentage, smooth transition. Animation: segments animate in clockwise on load using animationDuration={500}, startAngle and endAngle transitions. Click handler: onClick on segment triggers filtering main dashboard data by that category, visual selected state (stroke or scale). Responsive: smaller size on mobile (150-200px), simplified labels (legend only), adjust center text size. Color palette: use consistent, accessible color scheme with sufficient contrast. Show "Other" segment: group small values (<5%) into "Other" to reduce clutter. Loading state: spinning circle placeholder. Empty state: "No data" with empty circle outline. Accessibility: provide data values in accessible format, keyboard navigation between segments. Dark mode: adjust colors and labels for dark background.'
      },
      {
        id: 'da6',
        label: 'Progress Bars',
        simple: 'Create progress bars showing completion percentage with labels.',
        detailed: 'Build progress bar components with multiple variants. Basic structure: track (bg-gray-200 rounded-full h-2), fill (bg-blue-600 rounded-full). Value display: percentage text to the right. Label: progress title above bar. Colors: different colors for status (green=complete, yellow=warning, red=critical). Animated fill: transition-all duration-500. Accessibility: role="progressbar" with aria attributes.',
        advanced: 'Build progress bar components with multiple variants and accessibility. Basic structure: track container (bg-gray-200 rounded-full), fill bar (rounded-full) positioned inside with width based on percentage. Value display: percentage text (text-sm font-medium) to the right, inside bar for thick variants, or as tooltip. Label: progress title (font-medium) above bar with optional subtitle. Size variants: thin (h-1), medium (h-2), thick (h-4 with text inside), with segments. Colors: contextual colors for status - green (bg-green-500) for complete/success, blue (bg-blue-500) for in-progress, yellow/amber (bg-amber-500) for warning, red (bg-red-500) for critical/error. Animated fill: transition-all duration-500 ease-out on width change for smooth animation. Striped pattern: CSS repeating-linear-gradient (45deg stripes) for active/processing state, animate stripe movement with CSS animation. Indeterminate: animated gradient or stripe movement for unknown progress duration, no percentage shown. Circular progress: ring-based alternative using SVG circle with stroke-dasharray and stroke-dashoffset, rotation animation, value in center. Multi-segment: different colored segments in one bar representing breakdown (e.g., 40% complete, 30% in-progress, 30% pending). Goal indicator: vertical marker line showing target position on track. Stacked progress bars: multiple bars in column for comparison with aligned labels. Buffer progress: secondary lighter fill showing buffered amount (like video loading). Accessibility: role="progressbar", aria-valuenow={value}, aria-valuemin={0}, aria-valuemax={100}, aria-label describing what progress represents. Dark mode: adjust track color (dark:bg-gray-700). Component API: value, max, label, color, size, showValue, animated, indeterminate props.'
      },
      {
        id: 'da7',
        label: 'Activity Feed',
        simple: 'Create activity feed showing recent events with timestamps and user avatars.',
        detailed: 'Create activity feed/timeline component. Container: vertical list with max-height and scroll. Item structure: avatar (small), content (flex-1), timestamp. Timeline line: vertical connecting line between items. Event types: different icons/colors for create, update, delete, comment. Content: user name (font-medium), action description. Relative timestamps: "2 minutes ago". Grouping: group by date with headers. Load more button or infinite scroll.',
        advanced: 'Create activity feed/timeline component with real-time updates. Container: vertical list with max-height (max-h-96 or custom) and overflow-y-auto, custom scrollbar styling. Item structure: flex row with avatar (w-8 h-8 rounded-full) on left, content (flex-1) in middle, timestamp (text-xs text-gray-400) on right or below content. Timeline line: vertical connecting line between items using pseudo-elements (::before on item with absolute positioning, w-0.5 bg-gray-200) or left border on container. Event types: different icons with colored backgrounds (w-8 h-8 rounded-full flex items-center justify-center) replacing avatar - PlusCircle (green) for create, Edit (blue) for update, Trash (red) for delete, MessageCircle (purple) for comment, User (gray) for user actions. Content: user name (font-medium text-gray-900) as link, action description ("created a new project", "updated the status"), affected item as link (text-blue-600 hover:underline). Relative timestamps: use date-fns formatDistanceToNow or similar ("2 minutes ago", "Yesterday at 3:45 PM", "Jan 15"), show full date on hover with title attribute. Grouping: group by date with sticky date headers ("Today", "Yesterday", "January 15, 2024"). Load more: "Load more" button at bottom, loading state with spinner, or infinite scroll using IntersectionObserver. Filters: filter pills or dropdown to filter by event type or user. Mark as read: visual distinction for new vs seen items (bg-blue-50 for unread), dot indicator. Real-time: new items appear at top with highlight animation (bg-yellow-50 fade out), use WebSocket or polling. Empty state: "No recent activity" message with illustration. Click item: navigate to related content. Hover: show additional details or quick actions. Collapse long descriptions with "Show more". Dark mode support. Accessibility: list role, announce new items with aria-live.'
      },
      {
        id: 'da8',
        label: 'Data Table',
        simple: 'Create data table with sortable columns and pagination.',
        detailed: 'Build comprehensive data table for dashboard. Container: overflow-x-auto wrapper. Header row: sticky top, sortable columns (click to sort with direction arrow). Body rows: zebra striping or hover highlight. Cells: proper alignment, truncate long text. Row selection: checkbox column with bulk actions. Pagination: page numbers and items per page dropdown. Search: global search input. Loading: skeleton rows. Empty: helpful message.',
        advanced: 'Build comprehensive data table for dashboard with full functionality. Container: overflow-x-auto wrapper for horizontal scroll on small screens, bg-white rounded-lg shadow-sm. Header row: sticky top (position: sticky top-0 z-10 bg-white), sortable columns with click handler (show ChevronUp/ChevronDown icon for sort direction, ChevronUpDown for unsorted), column resize handles (draggable dividers between headers, cursor-col-resize). Column header: font-medium text-gray-700, whitespace-nowrap, optional info tooltip. Body rows: zebra striping (even:bg-gray-50) or hover highlight (hover:bg-gray-50), transition for smooth hover. Border options: divide-y divide-gray-200 or individual cell borders. Cells: proper alignment (numbers and currency right-aligned text-right, dates center, text left), truncate long text with text-ellipsis overflow-hidden and tooltip showing full value, proper padding (px-4 py-3). Row selection: checkbox column as first column, "Select all" checkbox in header (with indeterminate state when partial), bulk actions toolbar appears (fixed or inline) when rows selected showing count and action buttons (Delete, Export, etc.). Pagination: below table in card footer, previous/next buttons with ChevronLeft/ChevronRight, page number buttons with ellipsis, items per page dropdown (10, 25, 50, 100), "Showing X-Y of Z results" text. Search: global search input above table, debounced (300ms), highlights matching terms in results. Filters: column-specific filters in header row (dropdown or popover) or separate filter panel, active filters shown as pills. Column visibility: dropdown menu to show/hide columns, drag to reorder. Export: download as CSV/Excel button using file-saver and xlsx libraries. Loading: skeleton rows (5-10) with animate-pulse or overlay spinner. Empty state: centered message "No data found" with optional action button. Responsive: horizontal scroll default, or transform to card view on mobile (each row becomes a card). Row click: optional row click handler for navigation. Inline editing: optional cell editing on double-click. Dark mode support. Accessibility: proper table semantics, sortable column announcements, row selection announcements.'
      },
      {
        id: 'da9',
        label: 'Status Overview',
        simple: 'Create status blocks showing counts by state (Active, Pending, Complete, etc.).',
        detailed: 'Create status overview component showing counts by state. Layout: horizontal row of status blocks. Each block: status icon with color, count (large, bold), status label. Status types: Active (green), Pending (yellow), Completed (blue), Failed (red). Visual indicators: colored left border or icon color. Click to filter: clicking filters the main list. Animation: counts animate up on load.',
        advanced: 'Create status overview component showing counts by state with interactivity. Layout: horizontal row (flex gap-4) of status blocks or cards, wrap to multiple rows on mobile (flex-wrap). Each block/card: bg-white rounded-lg p-4 shadow-sm hover:shadow-md transition-shadow cursor-pointer. Content: status icon (CircleCheck, Clock, AlertCircle, XCircle) with status-specific color in small circle background, count value (text-2xl md:text-3xl font-bold), status label (text-sm text-gray-500). Status types with colors: Active (green-500, CheckCircle), Pending/In Progress (amber-500, Clock), Completed (blue-500, CircleCheck), Failed/Error (red-500, XCircle), Draft (gray-400, FileText), Cancelled (gray-500, Ban). Visual indicators: colored left border (border-l-4), colored icon, or subtle background tint (bg-green-50). Click to filter: clicking a status block filters the main table/list to show only items with that status, highlight selected status with ring-2 ring-offset-2. Active state: show which filter is currently applied. Percentage bars: optional horizontal bar below each showing proportion of total (w-full h-1 rounded-full bg-gray-200 with colored fill). Animation: counts animate up from 0 on load using requestAnimationFrame or framer-motion. Total: show total count in separate prominent block or as sum. Trend indicators: optional +5 or -3 badge showing change from previous period with colored arrow. Donut summary: optional small donut chart (40x40px) showing all proportions as visual summary. Responsive: 2 columns on tablet (grid-cols-2), stack on mobile or horizontal scroll. Interactive tooltips: hover shows additional details ("12 items, 15% of total"). Real-time updates: counts update without page refresh using polling or WebSocket, subtle flash animation on change. Dark mode: dark:bg-gray-800. Accessibility: each block is a button with aria-label describing action.'
      },
      {
        id: 'da10',
        label: 'Quick Stats',
        simple: 'Create stats row showing key metrics with labels and values.',
        detailed: 'Build quick stats row for key metrics at a glance. Layout: flex row with equal-width items. Stat item: icon (optional), label (text-sm text-gray-500), value (text-2xl font-bold), change indicator. Change indicator: arrow icon, percentage, color (green/red). Sparkline: optional mini chart. Responsive: 2 columns on tablet, stack on mobile. Loading state: skeleton placeholders.',
        advanced: 'Build quick stats row for key metrics at a glance with comprehensive features. Layout: flex row (flex-wrap or grid grid-cols-2 md:grid-cols-4) with equal-width items, gap-4 or gap-6, optional card backgrounds or dividers between items. Stat item structure: icon (optional, in w-10 h-10 rounded-full colored background circle, representing metric type), label (text-sm text-gray-500 uppercase tracking-wide), value (text-2xl md:text-3xl font-bold, properly formatted - currency with symbol, percentages with %, numbers with commas), change indicator row below. Change indicator: flex items-center gap-1, ArrowUp (text-green-600) or ArrowDown (text-red-600) icon, percentage change (+12.3%), color matching direction, comparison period text in parentheses ("vs last week"). Sparkline: optional inline mini chart (60x20px) showing 7-day trend, simple SVG path or tiny line chart, helps visualize trend beyond single number. Variants: simple text (no backgrounds), with card backgrounds (bg-white rounded-lg shadow-sm p-4), with vertical borders between items (divide-x), compact single-row. Click interaction: entire stat clickable, navigates to detailed view/report page. Loading state: skeleton placeholders matching layout (animate-pulse gray rectangles for value and label). Refresh button: optional RefreshCw icon button to manually refresh stats, loading state during refresh. Time range: selector dropdown or button group (Today, This Week, This Month, Custom) affecting all stats. Custom stat: "+" button to add custom tracked metric with configuration modal. Error state: show error icon with "Unable to load" message and retry. Real-time: optional live updates with subtle animation on change. Responsive: 2 columns on tablet (sm:grid-cols-2), stack to single column on mobile. Dark mode: dark:bg-gray-800 dark:text-white. Accessibility: proper labeling, announce value changes with aria-live for real-time updates.'
      },
      {
        id: 'da11',
        label: 'User List',
        simple: 'Create user list with avatars, names, roles, and status badges.',
        detailed: 'Create user list component for team management. List item: avatar (w-10 h-10 rounded-full), name (font-medium), email or role, status badge, actions. Status badges: Active (green), Invited (yellow), Inactive (gray). Actions: dropdown menu with Edit, Deactivate, Remove. List header: search input, role filter, Add User button. Sorting: by name, date, status. Pagination for long lists. Empty state: "No users found".',
        advanced: 'Create user list component for team or customer management with full features. List layout: table-style rows or card grid variant. List item row: flex items-center gap-4 py-4 border-b hover:bg-gray-50 transition. Content: avatar (w-10 h-10 rounded-full object-cover, fallback to initials in colored circle), name column (name as font-medium text-gray-900, email below as text-sm text-gray-500, or role), status badge, date added, actions. Status badges: Active (bg-green-100 text-green-800), Invited/Pending (bg-amber-100 text-amber-800), Inactive (bg-gray-100 text-gray-600), Admin (bg-purple-100 text-purple-800). Badge styling: px-2 py-0.5 text-xs font-medium rounded-full. Actions: dropdown menu (MoreHorizontal trigger) with Edit Profile, Change Role, Resend Invite (if pending), Deactivate/Activate, Remove from team options, or inline icon buttons (Edit, Trash2). List header: flex justify-between items-center mb-4. Header left: search input (w-64, Search icon), role filter dropdown (All Roles, Admin, Member, Viewer). Header right: "Add User" or "Invite User" primary button. Sorting: click column headers to sort by name (A-Z, Z-A), date added (newest, oldest), status, role, last active. Sort indicator: ChevronUp/Down icon in header. Bulk selection: checkbox as first column, select all checkbox in header with indeterminate state, bulk actions toolbar (Change role, Remove selected) appears when rows selected. Pagination: for lists >20 users, page numbers or "Load more". Card variant: grid of user cards (grid-cols-1 sm:grid-cols-2 lg:grid-cols-3) with avatar centered, more profile details visible. Online status: green dot indicator (w-3 h-3 rounded-full bg-green-500 absolute) positioned on avatar corner for currently online users. Last active: "Active 2h ago" or "Last seen Jan 15" timestamp. Quick profile: hover card (absolute positioned) showing more user details without navigation. Empty state: illustration with "No users found" message, "Invite your first team member" CTA. Loading: skeleton rows with avatar circle and text lines. Search: filter list as user types (debounced). Dark mode support. Accessibility: table semantics, sortable column announcements, checkbox labels.'
      },
      {
        id: 'da12',
        label: 'Calendar View',
        simple: 'Create month calendar grid showing events on dates.',
        detailed: 'Build calendar component for date-based data. Month view: grid of 7 columns (days) x 6 rows (weeks). Header: month/year title, previous/next navigation arrows, Today button. Day cells: date number, event indicators (dots or text). Events: colored bars, click to view details. Day cell states: today (highlight), selected, other month (gray). Drag to reschedule events. Responsive: list view on mobile.',
        advanced: 'Build calendar component for date-based data with multiple views. Month view: grid of 7 columns (days of week) x 6 rows (weeks), using CSS grid or table. Header: month/year title (font-semibold text-xl), ChevronLeft/ChevronRight navigation arrows with hover states, "Today" button to jump to current date. Day of week headers: S M T W T F S (or full names), text-sm text-gray-500 text-center. Day cells: w-full aspect-square or fixed height (h-24 md:h-32), p-1, border-b border-r border-gray-200. Cell content: date number (text-sm font-medium) in corner, event indicators below. Date number positioning: top-left or centered. Events in cells: colored bars (h-5 rounded text-xs px-1 truncate) showing event title, different colors for categories, stacked vertically (show max 3, then "+2 more" link). Click event bar: opens event details modal. Day cell states: today (bg-blue-50 ring-2 ring-blue-500 ring-inset), selected (bg-blue-100), other month dates (text-gray-400), weekend (subtle bg tint optional). Click day cell: select date, optionally open day detail view. Week view: 7-day horizontal view with hour rows (24 or business hours) for time slots, events positioned by time, height based on duration. Day view: single day with detailed hour slots (30min or 1hr increments), events as positioned blocks. Mini calendar: small month view (150-200px) for sidebar date picker, click date navigates main calendar. Event creation: click empty time slot, or click and drag to create event spanning time, opens creation form modal with title, time, description, color fields. Event details: click event opens modal/popover with full details, Edit and Delete buttons. Drag to reschedule: drag events to different days/times, visual feedback during drag, confirm or auto-save on drop. Resize events: drag event edges to change duration. Responsive: on mobile, switch to agenda/list view showing upcoming events chronologically with date headers. Loading: skeleton calendar grid. Integration consideration: sync with external calendar services (Google, Outlook) via API. Dark mode support. Accessibility: proper grid semantics, keyboard navigation between cells/events, announce date changes.'
      },
      {
        id: 'da13',
        label: 'KPI Dashboard',
        simple: 'Create key performance indicator dashboard with metric cards.',
        detailed: 'Build KPI dashboard with summary metrics. Metric cards: grid of stat cards showing key values. Trends: sparklines or trend arrows showing direction. Comparison: show vs previous period. Color coding: green for positive, red for negative. Drilldown: click for detailed view. Real-time: auto-refresh for live data. Date range: selector for time period. Export: download report options.',
        advanced: 'Build comprehensive KPI dashboard for business metrics monitoring. Layout: grid of metric cards (grid-cols-2 lg:grid-cols-4), each card with consistent sizing. Card structure: icon/category indicator (colored circle), main value (text-3xl font-bold), label (text-sm text-gray-500), trend indicator (TrendingUp/Down with percentage). Value formatting: abbreviate large numbers (1.2M, 3.5K), proper currency symbols, percentage display, decimal precision based on metric type. Trend calculation: compare current to previous period, show percentage change (+12.5%, -8.3%), color coding (text-green-600 for positive, text-red-600 for negative). Sparkline trends: tiny inline line chart showing recent history (last 7/30 days), using recharts sparlines or custom SVG. Comparison period: "vs last week", "vs last month", "vs same period last year", show absolute and percentage difference. Target/goal: optional target indicator, progress bar showing % to goal, color based on on-track status. Drilldown: click card opens detailed view/modal with full chart, breakdown by category/region/product. Grouping: organize related KPIs into sections (Revenue, Users, Performance), section headers. Real-time updates: WebSocket or polling for live metrics, animate value changes, show last updated timestamp. Date range selector: preset ranges (Today, 7d, 30d, 90d, YTD, Custom), affects all dashboard metrics. Responsive: stack cards on mobile (grid-cols-1 sm:grid-cols-2), swipe between metric groups. Loading state: skeleton cards matching layout. Error handling: show error state per card if metric fetch fails. Export: download as PDF, CSV data export, schedule email reports. Favorites/pinning: let users pin important metrics to top. Alerts: configure threshold alerts (notify when metric exceeds/drops below value).'
      },
      {
        id: 'da14',
        label: 'Analytics Overview',
        simple: 'Create analytics page with traffic and user metrics.',
        detailed: 'Build analytics overview dashboard. Traffic metrics: page views, sessions, users. Charts: line chart for trends, bar chart for comparisons. Sources: traffic source breakdown (pie chart). Top pages: table of most viewed pages. User flow: funnel visualization. Devices: mobile vs desktop split. Geography: map showing user locations. Real-time: current active users count.',
        advanced: 'Build comprehensive analytics overview dashboard like Google Analytics. Key metrics row: Sessions, Users, Page Views, Bounce Rate, Avg Session Duration as stat cards with sparklines. Main chart: area/line chart showing primary metric over time, multiple series for comparison (current vs previous period as dashed line), zoom/pan for detailed inspection, hover shows tooltip with values. Time range selector: Today, Yesterday, Last 7/30/90 days, Custom range picker, comparison toggle. Traffic sources: pie or donut chart showing Direct, Organic Search, Social, Referral, Email, display percentages and absolute numbers. Top pages table: ranked list of pages by views, columns: Page, Views, Unique Views, Avg Time, Bounce Rate, sortable columns, link to page detail. Users section: new vs returning donut, active users line chart, user acquisition by source. Real-time panel: current active users count (updating live), pages being viewed now, geographic pins. Device breakdown: horizontal bars for Desktop, Mobile, Tablet percentages. Geographic: world map with country heatmap, country list with metrics, drill down to cities. Browser/OS: breakdown charts. Behavior flow: funnel visualization from landing to conversion. Engagement: scroll depth, click patterns if available. Segments: filter all data by segment (new users, mobile, organic). Export: CSV export, scheduled reports, custom dashboards. Loading: show progress for data-heavy queries. Empty state: show when no data for selected period.'
      },
      {
        id: 'da15',
        label: 'Data Table Dashboard',
        simple: 'Create dashboard with searchable, filterable data table.',
        detailed: 'Build dashboard centered on data table. Table: sortable columns, pagination. Search: global search across all columns. Filters: column-specific filters. Actions: row actions (edit, delete). Selection: checkbox selection for batch actions. Export: download table data. Column visibility: show/hide columns. Responsive: horizontal scroll or card view on mobile.',
        advanced: 'Build comprehensive data table dashboard with advanced features. Table structure: clean table with proper spacing, header row with sortable columns, striped or hover rows. Sorting: click column header to sort, toggle asc/desc, multi-column sort with shift+click, show sort indicators (ChevronUp/Down). Search: global search input searching all text columns, debounced (300ms), highlight matches, clear button. Filters: per-column filters in header or filter panel, text filters (contains, equals), number filters (equals, greater/less than, range), date filters (range picker), select filters for enum columns, active filter indicators. Pagination: page size selector (10, 25, 50, 100), page number display, prev/next/first/last buttons, total row count. Row actions: actions column with dropdown or icon buttons, Edit, View, Delete, custom actions. Bulk selection: checkbox in header for select all (current page or all), checkbox per row, selection count display, bulk action buttons (Delete selected, Export selected). Column features: resizable column widths, reorderable via drag, hide/show columns toggle panel, sticky first column or actions column. Export: CSV download (current page or all, respecting filters), JSON option, print-friendly view. Virtualization: for large datasets, virtualize rows (react-window or tanstack-virtual). Loading: skeleton rows or loading overlay. Empty state: "No results" message with clear filters option. Error state: retry button on load failure. Mobile: responsive with horizontal scroll, or transform to card layout showing key fields. Keyboard navigation: arrow keys move between cells, Enter to edit. Customization: column configuration saved to localStorage. Integration: works with React Table, TanStack Table, or custom implementation.'
      },
      {
        id: 'da16',
        label: 'Multi-chart Dashboard',
        simple: 'Create dashboard with multiple chart types displaying data.',
        detailed: 'Build dashboard with various chart types. Chart grid: responsive grid of chart widgets. Chart types: line, bar, pie, area, doughnut. Legends: clear chart legends. Tooltips: hover tooltips with details. Interactivity: click to drill down. Loading: skeleton charts while loading. Responsive: charts resize appropriately. Date sync: shared date range affects all charts.',
        advanced: 'Build comprehensive multi-chart analytics dashboard. Grid layout: responsive CSS grid (grid-cols-1 md:grid-cols-2 xl:grid-cols-3), chart cards with consistent styling. Chart card component: white bg, rounded-xl, shadow-sm, p-4-6, title header, chart area, optional footer with actions. Chart types: Line (time series, trends), Area (stacked for composition over time), Bar (comparisons, rankings), Horizontal Bar (for long labels), Pie/Donut (part-to-whole), Radar (multi-variable comparison), Scatter (correlation). Chart library: Recharts for React, or Victory, Chart.js. Common styling: consistent color palette across all charts, same fonts, axis styling, legend positioning. Responsive charts: ResponsiveContainer wrapper, charts resize with container. Tooltips: custom tooltip component with card styling, show relevant metrics on hover. Legends: positioned below or right based on chart type, clickable to toggle series visibility. Interactions: click bar/pie slice for drilldown, zoom on line charts, selection for time ranges. Loading states: skeleton chart matching aspect ratio, or pulse placeholder. Error handling: error message in chart area with retry. Refresh: individual chart refresh button, auto-refresh interval option. Global date range: selector affects all charts, pass range as prop to each. Chart fullscreen: expand single chart to full screen for detailed view. Download: each chart has download as PNG/SVG option. Dashboard configuration: optional drag and drop reordering of chart positions, save layout preference. Dark mode: chart colors adapt to dark theme.'
      },
      {
        id: 'da17',
        label: 'Metrics Comparison',
        simple: 'Compare metrics between different time periods.',
        detailed: 'Build comparison view for metrics. Period selection: select two periods to compare. Side by side: show metrics for each period. Percentage change: calculate difference between periods. Visual comparison: bar charts or overlaid lines. Highlight changes: color code improvements vs declines. Metric selection: choose which metrics to compare. Export: comparison report export.',
        advanced: 'Build comprehensive metrics comparison dashboard. Comparison modes: period over period (this week vs last week), year over year (Dec 2024 vs Dec 2023), custom range vs custom range. Period selectors: two date range pickers, preset comparisons (vs Previous Period, vs Same Period Last Year). Metric display: side-by-side cards showing value for each period, percentage change calculated and displayed, absolute difference shown. Visual indicators: TrendingUp (green) for improvements, TrendingDown (red) for declines, neutral for minimal change, threshold for significance (ignore <1% changes). Comparison charts: overlaid line charts with different colors for each period, grouped bar charts for categorical data, toggle between overlay and side-by-side. Metric selection: checkbox list to choose which metrics to compare, category grouping (Revenue, Traffic, Conversions). Table format: rows as metrics, columns as periods and change, sortable by change percentage. Highlight significant changes: background highlight on metrics with large changes, configurable threshold (>10% change). Trend context: show if change is consistent with longer trend or anomaly. Breakdown comparison: compare by segment (by device, by country, by product), table showing each segment comparison. Statistical significance: for sample-based metrics, show confidence level. Notes/annotations: mark events that explain changes (holiday, campaign, bug fix). Report generation: generate comparison report, export as PDF or presentation, scheduled comparison emails. Goal comparison: compare actual vs target for each period. Insights: auto-generate text insights ("Revenue increased 15% driven by mobile users").'
      },
      {
        id: 'da18',
        label: 'Live Dashboard',
        simple: 'Create real-time dashboard with auto-updating metrics.',
        detailed: 'Build live updating dashboard. Real-time data: WebSocket or polling for updates. Animation: smooth transitions on data changes. Current status: show live values. Recent activity: stream of recent events. Pulse indicators: show active/live state. Connection status: indicate if connected. Auto-refresh: configurable refresh interval. Pause: ability to pause updates.',
        advanced: 'Build real-time dashboard with live updating data. Data connection: WebSocket for true real-time, or polling (5-30 second intervals). Connection status: indicator showing connected/disconnected/reconnecting, auto-reconnect with exponential backoff. Live metrics: current value updating in real-time, smooth number transitions (animate counting), pulse or glow effect on updates. Activity stream: feed of recent events/transactions, new items animate in at top, auto-scroll or "N new items" button, click to expand details. Current users: live count of active users, geographic distribution updating, active pages being viewed. Live chart: line chart that extends in real-time, rolling window (last 5 minutes), smooth animation for new data points. Threshold alerts: visual alert when metric exceeds threshold, color change, optional sound. Server status: health indicators for systems, green/yellow/red status, latency/response time. Pause/resume: button to pause updates, resume button shows backlog count. Refresh indicator: spinning icon or progress bar during data fetch, last updated timestamp. Buffer handling: handle data bursts, batch updates to prevent flickering. Reconnection: graceful handling of connection loss, show reconnecting status, fetch missed data on reconnect. Performance: efficient DOM updates, virtualize long lists, debounce rapid updates. Mobile: battery-conscious polling frequency, reduce update frequency in background. Keyboard shortcut: press to pause/resume. Sound: optional notification sounds for alerts. Full screen mode: distraction-free large display mode for monitoring.'
      },
      {
        id: 'da19',
        label: 'Funnel Analysis',
        simple: 'Visualize conversion funnel with step metrics.',
        detailed: 'Build funnel visualization for conversions. Steps: horizontal or vertical funnel stages. Metrics: show count and percentage at each step. Drop-off: highlight drop-off between steps. Comparison: compare funnels for different segments. Time period: select date range for analysis. Drill down: click step for detailed breakdown. Benchmarks: show industry or historical benchmarks.',
        advanced: 'Build comprehensive funnel analysis visualization. Funnel structure: horizontal bars narrowing left to right, or vertical stacked segments. Step data: step name, count at step, percentage of previous step, percentage of total (entry). Drop-off highlighting: show drop-off percentage between steps (red text or bar), visualize lost users at each transition. Funnel shape: proportional widths based on actual numbers, or fixed step heights with overlay fill. Step details: hover for tooltip with full metrics, click for drilldown to user list or sub-funnel. Time-based: select date range, see funnel for that period, compare to previous period. Segmentation: compare funnels side by side (mobile vs desktop, new vs returning), segment selector dropdown. Cohort funnels: track specific cohort through funnel over time. Step timing: average time between steps, time distribution histogram. Goal conversion rate: overall funnel conversion rate prominently displayed, trend vs previous period. Breakdowns: within each step, see breakdown by dimension (source, device, campaign). User paths: show alternate paths users take (not just linear). Exclusions: ability to exclude segments from funnel. Annotations: mark events that affected funnel (feature launch, bug). Benchmarks: optional benchmark line showing target or industry average. Export: funnel data as CSV, funnel image as PNG. Interactive: click and drag to reorder steps for ad-hoc analysis. A/B comparison: compare funnel between experiment variants. Alerts: notify when step conversion drops below threshold.'
      },
      {
        id: 'da20',
        label: 'Geographic Dashboard',
        simple: 'Show data visualized on geographic map.',
        detailed: 'Build geographic data visualization. World map: country-level data visualization. Choropleth: color intensity based on values. Markers: data point markers with popups. Zoom: zoom and pan controls. Legend: color scale legend. Selection: click region for details. Data overlay: bubble sizes or heatmap. Filter: filter by region or metric.',
        advanced: 'Build comprehensive geographic data dashboard. Map options: world map for global, country map for regional, city-level for local data. Choropleth coloring: fill regions based on metric value, color scale from light to dark (or diverging scale), legend showing value ranges. Map library: react-simple-maps, mapbox-gl, or leaflet. Markers: circular markers for point data, size based on value (bubble map), color coding for categories, clustering for dense areas. Popups/tooltips: hover region shows metric values, click for detailed modal. Zoom and pan: smooth zoom with scroll, pan by drag, zoom controls (+/-), fit to data bounds. Legend: color scale legend, interactive (hover to highlight regions). Region selection: click region to select, show details panel, multiple selection for comparison. Overlay options: toggle between choropleth, bubble, heatmap. Multiple metrics: switch between metrics to visualize, optional bivariate choropleth. Time animation: slider to animate through time periods, play/pause, see changes over time. Data table sync: table of regions alongside map, highlight corresponding map region on row hover. Search: search for region by name, zoom to found region. Export: download map as image, export underlying data. Custom boundaries: support for custom geographic boundaries (sales territories). Offline support: map tiles cached for offline use. Accessibility: provide data table alternative for non-visual access. Drill down: click country to zoom to states, state to cities. Comparison: split view comparing two maps side by side.'
      },
      {
        id: 'da21',
        label: 'User Activity Timeline',
        simple: 'Show timeline of user activities and events.',
        detailed: 'Build user activity timeline. Vertical timeline: chronological list of activities. Event types: different icons/colors per type. Time grouping: group by day or session. Details: expandable details per event. Filtering: filter by activity type. User context: show user info at top. Infinite scroll: load more as scrolling. Export: download activity log.',
        advanced: 'Build comprehensive user activity timeline for user analysis. Timeline layout: vertical chronological list, newest at top, date/time separators for grouping. Activity item: timestamp (relative or absolute), icon for activity type, activity description, additional context (IP, device, location). Activity types: logins/logouts, page views, clicks, form submissions, transactions, errors, support interactions, each with distinct icon and color. Expandable details: click to expand full activity details, request/response data (sanitized), related items. User header: user profile card at top, avatar, name, email, key user metrics (total activities, last active, lifetime value). Filtering: filter by activity type (checkboxes), date range picker, search within activities. Session grouping: group activities into sessions, collapsed session summary with expand. Infinite scroll: load 50 activities initially, load more on scroll, loading indicator. Empty state: "No activities in selected period". Export: download as CSV, full log or filtered view. Privacy: hide/mask sensitive data (passwords, tokens), access controls on who can view. Comparison: optionally compare to typical user behavior. Related users: link to related users (same IP, same company). Anomaly highlighting: flag unusual activities (suspicious login, error spike). Live mode: for support, show activities updating in real-time. Integration: link to related records (order details, support ticket). Time zone: display in user local time or viewer time with toggle.'
      },
      {
        id: 'da22',
        label: 'Goal Tracking',
        simple: 'Track progress toward goals with visual indicators.',
        detailed: 'Build goal tracking dashboard. Goals list: cards or rows for each goal. Progress: progress bar or ring. Metrics: current value vs target. Timeline: due date and time remaining. Status: on track, at risk, behind indicators. Milestones: intermediate checkpoints. History: progress over time chart. Actions: buttons to update progress.',
        advanced: 'Build comprehensive goal tracking dashboard for OKRs or targets. Goal card: goal title and description, owner (avatar and name), target value and current value, progress indicator (bar or circular), due date and time remaining. Progress visualization: horizontal progress bar with percentage, or circular progress ring, color changes based on status (green on track, yellow at risk, red behind). Status calculation: compare current progress to expected progress (linear interpolation to deadline), determine on track/at risk/behind. Status badges: colored badge with icon (CheckCircle, AlertTriangle, XCircle). Milestones: intermediate checkpoints with dates, show milestone progress (3 of 5 completed), milestone icons on progress bar. Trend chart: line chart showing progress over time, target line for reference, projected completion based on current rate. Goal hierarchy: support OKR structure (Objectives with Key Results), expandable parent goals, roll-up progress from child goals. Filters: by owner, team, status, time period. Add/edit goals: form to create new goals, inline editing for updates. Progress updates: log progress entries with date and notes, update history visible. Notifications: alert when goal at risk, reminder before deadline. Scoring: optional weighted scoring for multiple goals, overall score display. Alignment: show how goals align to company objectives. Retrospective: after deadline, mark as achieved/missed, capture learnings. Export: goal report export, progress summary PDF. Gamification: optional badges for achieving goals.'
      },
      {
        id: 'da23',
        label: 'Notification Center',
        simple: 'Create notification center for dashboard alerts.',
        detailed: 'Build notification center component. Bell icon: shows unread count badge. Dropdown: list of notifications. Types: info, warning, success, error with icons. Unread: highlight unread notifications. Mark read: click to mark as read. Actions: action buttons in notifications. Clear all: dismiss all notifications. Settings: notification preferences link.',
        advanced: 'Build comprehensive notification center for dashboard. Trigger: Bell icon in header with unread count badge (Badge component), red bg for alerts, blue for info. Dropdown panel: positioned absolute right, w-80 or w-96, max-h-96 overflow-y-auto, shadow-lg, rounded-lg. Header: "Notifications" title, "Mark all read" button, Settings gear icon link. Notification item: icon (type-specific), title text, body text (truncated), timestamp, unread indicator (dot or background), hover highlight. Types: info (InfoCircle, blue), success (CheckCircle, green), warning (AlertTriangle, yellow), error (XCircle, red), action required (bold styling). Unread styling: bg-blue-50 or left border, dot indicator. Read state: click notification marks as read, dimmer styling for read. Actions: optional action buttons per notification, "View", "Dismiss", custom actions. Grouping: group by date (Today, Yesterday, Earlier), or by category. Empty state: "No notifications" with illustration. Real-time: new notifications push to top with animation, update badge count. Notification preferences: settings modal or page, toggle types, delivery method (in-app, email, push). Mark all read: button in header, confirmation for many unread. Auto-dismiss: some notifications auto-dismiss after viewing. Persistence: notifications stored in database, sync across sessions. Mobile: full-screen modal on mobile, adequate touch targets. Accessibility: aria-live for new notifications, proper labeling. Sound: optional sound on new notification. Archive: view old/dismissed notifications.'
      },
      {
        id: 'da24',
        label: 'Report Builder',
        simple: 'Create interface for building custom reports.',
        detailed: 'Build report builder interface. Metrics selection: choose which metrics to include. Dimensions: select grouping dimensions. Filters: add filters to narrow data. Visualization: choose chart type. Layout: arrange components. Preview: see report preview. Save: save report configuration. Schedule: set up recurring reports. Export: download in various formats.',
        advanced: 'Build comprehensive report builder for custom analytics. Report configuration: metric selector (multi-select from available metrics), dimension selector (group by fields), filter builder (add conditions). Drag and drop: drag metrics/dimensions into report areas, remove by dragging out or X button. Filter builder: add filter rows, select field, operator (equals, contains, greater than), value input, AND/OR logic, nested conditions. Visualization options: chart type selector (table, line, bar, pie, etc.), chart-specific options (stacking, orientation). Layout builder: drag and drop widgets on canvas grid, resize widgets, multi-column layouts. Live preview: preview updates as configuration changes, loading indicator for queries. Query builder: optional SQL mode for advanced users, validate query syntax. Save report: name and description, save to personal or shared reports, folder organization. Report templates: start from templates, share report configs. Sharing: share report with team members, public link option. Schedule: set up recurring email delivery, frequency (daily, weekly, monthly), recipient list, delivery time. Export formats: PDF, CSV, Excel, PNG for charts. Parameters: date range selector on saved reports, user-input parameters. Favorites: mark frequently used reports. Recent reports: quick access to recently viewed. Permissions: control who can edit vs view reports. Versioning: track report configuration changes. Embedding: generate embed code for external use.'
      },
      {
        id: 'da25',
        label: 'Cohort Analysis',
        simple: 'Analyze user behavior by cohort groups.',
        detailed: 'Build cohort analysis dashboard. Cohort table: matrix of cohorts and time periods. Color coding: color intensity shows retention. Cohort size: show original cohort size. Metric: select retention metric. Time granularity: day, week, month. Comparison: compare cohorts. Filters: segment cohorts by attributes. Export: cohort data export.',
        advanced: 'Build comprehensive cohort analysis dashboard. Cohort table structure: rows are cohorts (grouped by signup date/month), columns are time periods after cohort start (Week 1, Week 2, etc.), cells show metric value (retention %, revenue, activity). Color coding: heatmap coloring (light to dark green for high retention), configurable color scale. Cell values: percentage for retention, absolute values for other metrics, hover tooltip for full details. Cohort definition: default by signup date, custom cohorts (first purchase, feature adoption), cohort size shown in first column. Metrics: retention rate (% still active), average revenue per user, conversion rate, any custom event. Time granularity: daily (first 14 days), weekly, monthly, configurable. Column display: show actual dates or relative periods. Trend lines: toggle row charts showing cohort trend, average line across all cohorts. Comparison: highlight selected cohort vs average, compare two cohorts side by side. Filters: filter users in cohorts by segment (channel, plan, location). Statistical significance: indicate if differences are significant. Export: download cohort table as CSV, or as image. Benchmarks: show target retention rates as reference. Insights: auto-generate text insights about trends. Drill down: click cell to see users in that cohort/period, user list with actions. Time range: select analysis period, update cohort inclusion. Predictions: project future cohort performance based on patterns. Mobile: scrollable table with frozen first column.'
      },
      {
        id: 'da26',
        label: 'Leaderboard',
        simple: 'Create ranked leaderboard for metrics or performance.',
        detailed: 'Build leaderboard component. Ranking: numbered list by metric. Avatars: user or team avatars. Metrics: show relevant performance metrics. Changes: up/down arrows for position changes. Top highlight: special styling for top performers. Filtering: filter by period or category. Self position: highlight current user. Pagination: for long leaderboards. Animations: smooth rank change animations.',
        advanced: 'Build comprehensive leaderboard for performance ranking. Layout: table or card list with rank numbers, user/team avatar, name, primary metric, secondary metrics. Ranking display: #1, #2, #3 with special styling (gold, silver, bronze badges or background), numerical rank for others. Metric display: primary metric prominently shown, formatted appropriately (currency, percentage, count), secondary metrics in smaller columns. Position changes: arrow icons showing up/down movement since last period, +3 or -2 change values, new entries marked. Podium view: optional podium visualization for top 3 (elevated cards, #1 center and larger). Filtering: filter by time period (today, this week, this month, all time), filter by category or segment, search by name. Self highlighting: current user row highlighted (bg-blue-50), show "You" badge, always visible even if scrolled. Pagination: show top 50-100, load more button, or virtualized for long lists. Animations: smooth transitions when ranks change, items slide into new positions. Ties: handle tied ranks (two #3s then #5). Historical: toggle to see leaderboard at previous points in time. Multiple boards: tabs for different metric leaderboards. Goals: optional goal indicator per user (% of target). Profile links: click name to view full profile. Export: download leaderboard as CSV. Privacy: option to show anonymous leaderboard (hide names). Gamification: optional badges, achievements, streaks displayed.'
      },
      {
        id: 'da27',
        label: 'Error Dashboard',
        simple: 'Monitor and display application errors.',
        detailed: 'Build error monitoring dashboard. Error list: recent errors with timestamps. Grouping: group by error type/message. Counts: show occurrence frequency. Trends: error trend chart. Stack traces: expandable stack traces. Status: acknowledged, resolved, ignored. Affected users: count of impacted users. Quick actions: ignore, resolve, create issue.',
        advanced: 'Build comprehensive error monitoring dashboard like Sentry. Error grouping: group similar errors by message signature, fingerprint unique errors, show occurrence count per group. Error list: table with error message (truncated), type/category, count, affected users, last seen, first seen, status. Filtering: by error type, status (unresolved, resolved, ignored), date range, severity level, environment (production, staging). Sorting: by count, recency, user impact, severity. Error detail view: full error message, stack trace (syntax highlighted, collapsible frames), related errors. Context: request URL, user info (ID, browser, OS), session replay link if available. Affected users: list of users who encountered error, count and percentage of total users. Trend chart: occurrences over time, helps identify if growing or one-time. Status workflow: open, acknowledged (being investigated), resolved, ignored (will not fix), snooze until occurs again. Quick actions: mark resolved, ignore, assign to team member, create GitHub issue, copy error details. Alerting: configure alerts for new errors or error spikes, notification integrations (Slack, email). Release tracking: tag errors by release version, identify regression vs existing. Aggregation: error count KPI card, errors by release, errors by route. Real-time: new errors appear automatically. Search: full-text search across error messages and stack traces. Bulk actions: resolve or ignore multiple selected errors. Integration: link to code (click stack frame opens code), source maps support.'
      },
      {
        id: 'da28',
        label: 'Audit Log Viewer',
        simple: 'Display audit trail of system activities.',
        detailed: 'Build audit log viewer. Log entries: list of actions with details. Filters: filter by user, action type, date. Search: search log entries. Actor: who performed the action. Target: what was affected. Changes: before/after values for edits. Timestamp: precise time of action. Export: download audit log. Pagination: handle large log volumes.',
        advanced: 'Build comprehensive audit log viewer for compliance and debugging. Log entry structure: timestamp (with timezone), actor (user or system), action type (verb), resource type and ID, changes (before/after), IP address, user agent. List display: table with key columns, expandable rows for full details. Action types: create, update, delete, read (if logged), login/logout, permission changes, settings changes. Filtering: by actor (user dropdown), by action type (checkboxes), by resource type, by date range, by IP. Search: full-text search across entries, search by resource ID. Change display: JSON diff view or formatted table showing changed fields, before values (red), after values (green). Related entries: link to related log entries (all actions on same resource). Export: CSV or JSON export, filtered or all, scheduled exports for compliance. Retention: display entries within retention period, indicate if older logs archived. Immutability: logs are read-only, cannot be edited or deleted (display only). Volume handling: pagination or virtual scroll for millions of entries, lazy loading. Aggregation: summary stats (actions per day, top actors), optional charts. Real-time: new entries appear at top (optional auto-refresh). Access control: audit log access is itself audited, restricted to admin roles. Compliance: GDPR/SOC2 friendly presentation, redact sensitive values. API access: programmatic access to audit logs. Anomaly detection: highlight unusual activity patterns. Print view: formatted print-friendly view for compliance documentation.'
      },
      {
        id: 'da29',
        label: 'Health Monitor',
        simple: 'Monitor system health with status indicators.',
        detailed: 'Build system health monitoring dashboard. Services: list of monitored services. Status: green/yellow/red indicators. Uptime: uptime percentage display. Latency: response time metrics. Incidents: current and past incidents. Notifications: alert on status changes. History: status history timeline. Dependencies: show service dependencies.',
        advanced: 'Build comprehensive system health monitoring dashboard. Service list: grid or list of all monitored services/components, each with name, status indicator, brief description. Status indicators: green (operational), yellow (degraded/slow), red (outage/error), gray (maintenance/unknown), pulsing animation for active issues. Status summary: header showing overall status, count of issues. Uptime display: percentage uptime (99.9%), uptime over different periods (24h, 7d, 30d, 90d), uptime bar visualization showing history. Latency metrics: current response time (p50, p95, p99), latency trend chart, threshold indicators (normal, elevated, critical). Service detail: click service for detail modal, full metrics, recent incidents, maintenance schedule. Incident management: current incident banner at top, incident timeline with updates, severity level and affected services. Incident history: list of past incidents, postmortem links, filter by severity and service. Status page: optional public status page view, subscribe to updates. Notifications: configure alert channels (email, Slack, PagerDuty), alert thresholds, on-call schedule. Dependency map: visualization of service dependencies, trace impact of issues. Metrics charts: CPU, memory, error rates, request volume, per-service charts. Maintenance windows: schedule and display planned maintenance. Auto-recovery detection: detect and show when issue auto-resolved. SLA tracking: service level agreement compliance, remaining error budget. Export: status reports, incident reports. Mobile: responsive for on-call mobile access, push notifications.'
      },
      {
        id: 'da30',
        label: 'Task Board',
        simple: 'Create kanban-style task board for project management.',
        detailed: 'Build task board with columns. Columns: status columns (To Do, In Progress, Done). Cards: task cards with title and metadata. Drag and drop: move cards between columns. Quick add: add new tasks inline. Card details: click for full task details. Filters: filter by assignee, label. Swimlanes: optional row grouping. WIP limits: warn when column has too many items.',
        advanced: 'Build comprehensive kanban-style task board for project management. Board structure: horizontal scrollable columns, each column is a status (Backlog, To Do, In Progress, Review, Done), column header with title and task count. Column features: add new column, rename column, delete empty column, set WIP limit, collapse/expand column. Task cards: title (1-2 lines), labels (colored tags), assignee avatar(s), due date, comment count, priority indicator. Card actions: click for detail modal, drag and drop to move, quick edit on hover. Drag and drop: smooth dnd with visual feedback, drop zones highlight, reorder within column, move between columns. Quick add: input field at bottom of column, press Enter to add, or dedicated Add button. Task detail modal: full description (markdown support), comments thread, attachments, activity log, subtasks, custom fields. Filters: assignee filter, label filter, due date filter, search by title, my tasks toggle. Swimlanes: optional horizontal grouping by assignee, priority, or custom field, collapsible swimlanes. WIP limits: configure max cards per column, visual warning when limit exceeded (red border), encourage finishing over starting. Sorting: within column sort by due date, priority, or manual order. Labels: create and manage labels, multi-select on cards. Bulk actions: select multiple cards, bulk move or edit. Views: toggle between board and list view. Keyboard shortcuts: arrow keys navigate cards, Enter opens, quick keys for actions. Real-time: updates sync across users, show other users viewing/editing. Templates: save board as template, create from template.'
      },
      {
        id: 'da31',
        label: 'Resource Usage',
        simple: 'Display system resource usage metrics.',
        detailed: 'Build resource usage dashboard. CPU: usage percentage with gauge. Memory: used/total with bar. Storage: disk usage breakdown. Network: bandwidth usage. Processes: top resource-consuming processes. Alerts: threshold warning indicators. History: usage over time charts. Comparison: compare to capacity limits.',
        advanced: 'Build comprehensive resource usage dashboard for infrastructure monitoring. Metric gauges: circular or semi-circular gauges for CPU %, Memory %, Disk %, each with value label, color zones (green <70%, yellow 70-90%, red >90%). Gauge details: show used/total (8 GB / 16 GB), percentage prominently displayed. Memory breakdown: used, cached, available, swap, stacked bar or breakdown chart. CPU breakdown: by core if multi-core, user vs system vs idle, load averages (1m, 5m, 15m). Disk/storage: per-volume breakdown, used/total/available, inode usage if relevant, growth rate projection. Network: inbound/outbound bandwidth, packets/second, current active connections, interface breakdown. Process table: top 10 processes by CPU or memory, columns: PID, name, CPU %, memory %, user, sortable. Historical charts: line charts showing metric over time, selectable time range (1h, 6h, 24h, 7d), zoom and pan. Alerts: visual indicator when threshold exceeded, configurable thresholds, active alerts panel. Capacity planning: current vs capacity limit, projection when capacity reached, recommendation prompts. Comparison: compare to previous period, compare to other servers in cluster. Auto-refresh: real-time updates every 5-10 seconds, last updated timestamp. Server selection: for multiple servers, dropdown to select which to view, or overview dashboard. Export: download usage data, generate capacity report. Responsive: dashboard adapts to different screen sizes. Tooltips: detailed info on hover for each metric.'
      },
      {
        id: 'da32',
        label: 'Dashboard Customization',
        simple: 'Let users customize their dashboard layout.',
        detailed: 'Build customizable dashboard. Widget library: available widgets to add. Drag and drop: arrange widgets on grid. Resize: resize widget dimensions. Remove: delete unwanted widgets. Settings: per-widget configuration. Layouts: save and switch layouts. Defaults: reset to default layout. Sharing: share custom layouts with others.',
        advanced: 'Build comprehensive dashboard customization system. Widget library: panel or modal showing available widgets (charts, stats, lists, etc.), search/filter widgets, categorized by type. Widget catalog: each widget has name, description, preview, size requirements. Drag and drop: drag from library to dashboard grid, visual drop zones, snap to grid. Grid system: responsive grid (12 columns typically), widgets occupy grid cells (2x2, 4x2, etc.), minimum and maximum sizes per widget type. Resize handles: corner and edge handles to resize widgets, respect min/max constraints, show ghost during resize. Widget reordering: drag existing widgets to new positions, other widgets reflow, smooth animations. Remove widget: X button on hover, or drag off dashboard, confirmation for widgets with configuration. Widget configuration: gear icon opens settings panel, widget-specific options (data source, chart type, filters), live preview of changes. Layout save: save current layout as named configuration, auto-save on changes (debounced), manual save option. Multiple layouts: create multiple dashboard layouts, quick switch between layouts, layout for different contexts (overview, detailed, mobile). Default layout: system default as starting point, reset to default option, admin-set defaults per role. Sharing: share custom layout with team or public, import shared layouts, clone and customize others layouts. Per-user: each user has own layout preferences, stored in database per user. Responsive: layouts adapt to screen size, or separate mobile layout. Lock layout: option to lock to prevent accidental changes. Template layouts: pre-designed layouts as starting points. Export/import: export layout config as JSON, import on different account.'
      }
    ]
  };
